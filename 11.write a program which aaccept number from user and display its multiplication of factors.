// 11.write a program which aaccept number from user and display its multiplication of factors.
//input:12
//output:144 (1* 2* 3* 4*6)

#include<stdio.h>
int MultFact(int ino)
{
	
	int i=0;
	int mult=1;
	for(i=1;i<ino;i++)
	{
		if(ino%i==0)
		{

		mult=mult*i;
	    
		}
	}
		return mult;

	
	}

int main()
{
	int ivalue=0;
	int iret=0;
	printf("Enter number");
	scanf("%d",&ivalue);
	iret=MultFact(ivalue);
	printf("%d",iret);
	return 0;
}
